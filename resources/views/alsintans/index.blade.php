<x-app-layout title="Data Alsintan"><x-slot:actions>@if(auth()->user()->hasRole('admin'))<a class="btn btn-primary" href="{{ route('alsintans.create') }}">＋ Tambah Alsintan</a>@endif</x-slot><section class="panel"><div class="panel-head"><h2>Database Alsintan</h2><form class="toolbar" method="get"><input class="search" name="search" value="{{ request('search') }}" placeholder="Cari jenis, merk, inventaris…"><button class="btn btn-outline">Filter</button></form></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Foto</th><th>Jenis</th><th>Merk / Tipe</th><th>Diterima Oleh (Poktan)</th><th>Kecamatan</th><th>Tahun Diterima</th><th>Aksi</th></tr></thead><tbody>@forelse($alsintans as $item)<tr><td>@if($item->photo_path)<a href="{{ asset('storage/'.$item->photo_path) }}" target="_blank" rel="noreferrer"><img class="table-photo" src="{{ asset('storage/'.$item->photo_path) }}" alt="Foto {{ $item->type }}"></a>@else<span class="table-photo-empty">—</span>@endif</td><td><b>{{ $item->type }}</b><br><small>{{ $item->inventory_number ?: 'Tanpa nomor inventaris' }}</small></td><td>{{ $item->brand_type }}</td><td>{{ $item->poktan?->name ?? '—' }}</td><td>{{ $item->district }}</td><td>{{ $item->procurement_year }}</td><td><div class="row-actions"><a class="action" href="{{ route('alsintans.show',$item) }}" title="Lihat detail">⌕</a>@if(auth()->user()->hasRole('admin'))<a class="action" href="{{ route('alsintans.edit',$item) }}" title="Ubah data">✎</a><form method="post" action="{{ route('alsintans.destroy',$item) }}" data-confirm="Hapus data alsintan ini?">@csrf @method('delete')<button class="action" title="Hapus data">⌫</button></form>@endif</div></td></tr>@empty<tr><td colspan="7"><div class="empty"><div class="empty-icon">▣</div>Belum ada data alsintan.</div></td></tr>@endforelse</tbody></table></div>{{ $alsintans->links() }}</section></x-app-layout>
