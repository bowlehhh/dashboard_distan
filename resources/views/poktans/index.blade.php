<x-app-layout title="Data Poktan"><x-slot:actions><a class="btn btn-primary" href="{{ route('poktans.create') }}">＋ Tambah Poktan</a></x-slot><section class="panel"><div class="panel-head"><h2>Data Kelompok Tani</h2><form class="toolbar" method="get"><input class="search" name="search" value="{{ request('search') }}" placeholder="Cari nama atau ketua…"><select class="control" name="status"><option value="">Semua status</option><option @selected(request('status')==='Aktif')>Aktif</option><option @selected(request('status')==='Nonaktif')>Nonaktif</option></select><button class="btn btn-outline">Filter</button></form></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Nama Poktan</th><th>Ketua</th><th>Kecamatan</th><th>Komoditas</th><th>Anggota</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($poktans as $poktan)<tr><td><b>{{ $poktan->name }}</b></td><td>{{ $poktan->chairperson }}</td><td>{{ $poktan->district }}</td><td>{{ $poktan->commodity }}</td><td>{{ $poktan->member_count }}</td><td><x-badge :value="$poktan->status"/></td><td><div class="row-actions"><a class="action" href="{{ route('poktans.show',$poktan) }}">⌕</a><a class="action" href="{{ route('poktans.edit',$poktan) }}">✎</a><form method="post" action="{{ route('poktans.destroy',$poktan) }}" data-confirm="Hapus data kelompok tani ini?">@csrf @method('delete')<button class="action">⌫</button></form></div></td></tr>@empty<tr><td colspan="7"><div class="empty"><div class="empty-icon">◉</div>Belum ada data kelompok tani.</div></td></tr>@endforelse</tbody></table></div>{{ $poktans->links() }}</section></x-app-layout>
