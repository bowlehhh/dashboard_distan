<x-app-layout title="Tanaman Pangan"><x-slot:actions>@if(auth()->user()->hasRole('admin'))<a class="btn btn-primary" href="{{ route('crops.create') }}">＋ Tambah Data</a>@endif</x-slot><div class="stats"><x-stat-card label="Komoditas" :value="$summary->count()" caption="Tanaman pangan" icon="◆"/><x-stat-card label="Luas Tanam" :value="$summary->sum('planted_area')" caption="Hektar" icon="⌁"/><x-stat-card label="Produksi" :value="$summary->sum('production')" caption="Satuan produksi" icon="◇"/></div><div class="dashboard-grid"><section class="panel"><div class="panel-head"><h2>Luas Tanam per Komoditas</h2></div><div class="bar-chart">@forelse($summary as $item)<div class="bar"><b>{{ number_format($item->planted_area,0) }}</b><i style="height:{{ min(100,$item->planted_area/($summary->max('planted_area')?:1)*100) }}%"></i><span>{{ $item->commodity }}</span></div>@empty<div class="empty">Belum ada data.</div>@endforelse</div></section><section class="panel"><div class="panel-head"><h2>Produksi per Komoditas</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Komoditas</th><th>Luas (Ha)</th><th>Produksi</th></tr></thead><tbody>@foreach($summary as $item)<tr><td>{{ $item->commodity }}</td><td>{{ number_format($item->planted_area,2,',','.') }}</td><td>{{ number_format($item->production,2,',','.') }}</td></tr>@endforeach</tbody></table></div></section></div><section class="panel"><div class="panel-head"><h2>Data Tanaman Pangan</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Komoditas</th><th>Poktan</th><th>Kecamatan</th><th>Luas Tanam</th><th>Produksi</th><th>Periode</th><th>Aksi</th></tr></thead><tbody>@forelse($crops as $crop)<tr><td><b>{{ $crop->commodity }}</b></td><td>{{ $crop->poktan?->name ?? '—' }}</td><td>{{ $crop->district }}</td><td>{{ $crop->planted_area }} Ha</td><td>{{ $crop->production }} {{ $crop->unit }}</td><td>{{ $crop->period }}</td><td><div class="row-actions"><a class="action" href="{{ route('crops.show',$crop) }}" title="Lihat detail">⌕</a>@if(auth()->user()->hasRole('admin'))<a class="action" href="{{ route('crops.edit',$crop) }}" title="Ubah data">✎</a><form method="post" action="{{ route('crops.destroy',$crop) }}" data-confirm="Hapus data tanaman ini?">@csrf @method('delete')<button class="action" title="Hapus data">⌫</button></form>@endif</div></td></tr>@empty<tr><td colspan="7"><div class="empty">Belum ada data tanaman pangan.</div></td></tr>@endforelse</tbody></table></div>{{ $crops->links() }}</section></x-app-layout>
