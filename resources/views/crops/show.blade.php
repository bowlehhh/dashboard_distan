<x-app-layout title="Detail Tanaman Pangan"><x-slot:actions>@if(auth()->user()->hasRole('admin', 'penyuluh'))<a class="btn btn-primary" href="{{ route('crops.edit', $crop) }}">✎ {{ auth()->user()->hasRole('penyuluh') ? 'Input Hasil Panen' : 'Ubah Data' }}</a>@endif</x-slot><section class="panel"><div class="form-grid">@foreach(['Komoditas'=>$crop->commodity,'Kecamatan'=>$crop->district,'Luas Tanam'=>$crop->planted_area.' Ha','Luas Panen'=>$crop->harvested_area.' Ha','Produksi'=>$crop->production,'Periode'=>$crop->period,'Keterangan'=>$crop->notes] as $label=>$value)<div class="field"><label>{{ $label }}</label><div>{{ $value ?: '—' }}</div></div>@endforeach</div></section></x-app-layout>
