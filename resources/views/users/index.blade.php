<x-app-layout title="Pengguna"><x-slot:actions>@if(auth()->user()->hasRole('admin'))<a class="btn btn-primary" href="{{ route('users.create') }}">＋ Tambah Pengguna</a>@endif</x-slot><section class="panel"><div class="panel-head"><h2>Manajemen Pengguna</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Nama</th><th>Email</th><th>Peran</th><th>Unit Kerja</th><th>Status</th><th>Login Terakhir</th><th>Aksi</th></tr></thead><tbody>@forelse($users as $user)<tr><td><b>{{ $user->name }}</b></td><td>{{ $user->email }}</td><td>{{ $user->role === 'ppl' ? 'Penyuluh / PPL' : ucfirst($user->role) }}</td><td>{{ $user->unit_kerja ?: '—' }}</td><td><x-badge :value="$user->is_active ? 'Aktif' : 'Nonaktif'"/></td><td>{{ $user->last_login_at?->format('d/m/Y H:i') ?? 'Belum pernah' }}</td><td><div class="row-actions"><a class="action" href="{{ route('users.show',$user) }}" title="Lihat detail">⌕</a>@if(auth()->user()->hasRole('admin'))<a class="action" href="{{ route('users.edit',$user) }}" title="Ubah data">✎</a><form method="post" action="{{ route('users.destroy',$user) }}" data-confirm="Hapus akun ini?">@csrf @method('delete')<button class="action" title="Hapus akun">⌫</button></form>@endif</div></td></tr>@empty<tr><td colspan="7"><div class="empty">Belum ada pengguna.</div></td></tr>@endforelse</tbody></table></div>{{ $users->links() }}</section></x-app-layout>
