<x-app-layout title="Detail Pengguna"><section class="panel"><div class="form-grid">@foreach(['Nama'=>$user->name,'Email'=>$user->email,'Peran'=>ucfirst($user->role),'Unit Kerja'=>$user->unit_kerja,'Login Terakhir'=>$user->last_login_at?->format('d/m/Y H:i')] as $label=>$value)<div class="field"><label>{{ $label }}</label><div>{{ $value ?: '—' }}</div></div>@endforeach<div class="field"><label>Status</label><x-badge :value="$user->is_active ? 'Aktif' : 'Nonaktif'"/></div></div></section></x-app-layout>
